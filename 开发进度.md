# 《微光苔原 / Sporelight》开发进度

更新时间：2026-05-26

## 当前阶段

- 阶段：页面细化与结构重构
- 状态：已完成启动页新版重做与核心代码拆分，等待继续细化移动端表现与后续页面打磨
- 技术方案：Vite + TypeScript + Canvas
- 当前本地服务：`http://127.0.0.1:4173/`

## 已完成

- 阅读 `游戏初步想法.md`，确认游戏定位、主角设定、命名页规则、核心玩法“滚动 + 播种 + 共生”。
- 阅读 `全局设计规范.md`，确认 1280×720 画布、16:9 适配、UI 安全区和切图命名规则。
- 查看 `设计稿_v1`，确认已有页面：
  - 启动页
  - 输入玩家名
  - 主菜单
  - 关卡选择
  - 游戏界面
  - 游戏结算页面
  - 图鉴页
- 查看 `assets/images`，确认已有完整素材包和命名页素材包。
- 已整理第一版开发需求：`开发需求_v1.md`。

## 已确认资源

- 设置弹窗补充设计稿：`设计稿_v1/设置弹窗.png`。
- 设置弹窗补充图素目录：旧版为 `assets/images/sporelight_settings_popup_assets`，新版为 `assets/images/settings_popup`。

## 下一步建议

进入开发：

1. 初始化项目结构。
2. 接入资源路径配置。
3. 实现 16:9 页面适配和场景路由。
4. 实现存档模块。
5. 完成启动页、命名页、主菜单。
6. 完成关卡选择、第 1 关、结算页、图鉴页的完整闭环。

## 开发日志模板

后续每次开发请按此格式追加：

```md
### YYYY-MM-DD HH:mm

本次目标：
- 

完成内容：
- 

修改文件：
- 

验证结果：
- 

遗留问题：
- 

下一步：
- 
```

## 日志

### 2026-05-18 需求梳理

本次目标：
- 根据已有创意文档、设计稿和素材，整理一版可确认的 v1 开发需求。

完成内容：
- 输出 `开发需求_v1.md`。
- 新建本文件作为后续开发交接记录。

修改文件：
- `开发需求_v1.md`
- `开发进度.md`

验证结果：
- 已确认素材和页面设计稿目录存在。
- 尚未进行代码开发和浏览器验证。

遗留问题：
- 2026-05-19 已确认：默认名使用“露薇”；技术方案使用 Vite + TypeScript + Canvas；第 2、3 关 v1 锁定；设置弹窗需要开发。

下一步：
- 开始项目初始化与开发。

### 2026-05-19 需求确认

本次目标：
- 同步用户确认的 v1 开发范围。

完成内容：
- 默认名确认使用“露薇”。
- 技术方案确认使用 Vite + TypeScript + Canvas。
- 第 2、3 关确认 v1 锁定。
- 设置页确认需要按补充设计稿和图素开发弹窗。
- 已同步更新 `开发需求_v1.md`。

修改文件：
- `开发需求_v1.md`
- `开发进度.md`

验证结果：
- 文档层面已更新，代码开发尚未开始。

遗留问题：
- 无。

下一步：
- 初始化项目结构并实现第一版流程。

### 2026-05-19 初版流程开发

本次目标：
- 按已确认范围搭建 Vite + TypeScript + Canvas 初版游戏流程。

完成内容：
- 新增 Vite + TypeScript 项目结构。
- 使用现有 `assets` 作为 Vite 静态资源目录。
- 实现单页场景路由：启动页、命名页、主菜单、关卡选择、游戏、结算、图鉴。
- 实现 localStorage 存档：玩家名、关卡完成、图鉴解锁、设置项。
- 实现设置弹窗：音频滑块、画质选项、开关项、语言选择、重置名称、恢复默认设置、确认弹窗。
- 实现第 1 关 Canvas 原型：点击移动、WASD/方向键移动、E/按钮播种、两个播种点、生长反馈、出口完成。
- 第 2、3 关在关卡选择中保持锁定。
- 结算后解锁“发光苔藓”和“藤蔓桥”图鉴条目。

修改文件：
- `.gitignore`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `vite.config.ts`
- `index.html`
- `src/main.ts`
- `src/styles.css`
- `开发需求_v1.md`
- `开发进度.md`

验证结果：
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，构建通过。
- 本地服务 `http://127.0.0.1:4173/` 可访问。
- 浏览器验证通过：首次命名、主菜单、设置弹窗、关卡选择、第 1 关播种两次、结算、图鉴解锁。

遗留问题：
- 当前系统默认 Node 为 14.14.0，直接执行 `npm run build` 会因 Vite 5 需要较新 Node 而失败；已用 Codex bundled Node 验证通过。
- `bg_valley.png` 本身包含设计稿式 UI，Canvas 原型会叠加开发 HUD，后续如需更干净的游戏画面可补一张纯背景。
- v1 尚未加入真实音频文件，仅保留设置项与接口位置。

下一步：
- 进一步按设计稿细化页面布局、按钮图片状态和游戏场景纯背景表现。

### 2026-05-19 设置弹窗重做

本次目标：
- 根据 `设计稿_v1/设置弹窗.png` 和新目录 `assets/images/settings_popup` 的视觉风格，重做设置弹窗布局。

完成内容：
- 重构设置弹窗 DOM 结构，改为设计稿中的左侧角色陪伴区 + 右侧四个设置分区。
- 右侧分区调整为：音频、画面、操作、通用。
- 保留并适配原有交互：音量滑块、画质选项、动态光效、粒子效果、低动态模式、震动反馈、全屏模式、语言选择、重置名称、恢复默认设置。
- 底部按钮调整为“取消 / 恢复默认 / 确认”，并固定在弹窗底部。
- 对 1280×720 视口做了布局收束，避免右侧溢出和底部按钮掉出画面。

修改文件：
- `src/main.ts`
- `src/styles.css`
- `开发进度.md`

验证结果：
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，构建通过。
- 浏览器打开 `http://127.0.0.1:4173/` 验证设置弹窗，弹窗完整显示，主要控件可见，无明显重叠。

遗留问题：
- `assets/images/settings_popup` 当前是带文件名标注的组件总览图，不是逐个透明组件文件；本次主要按视觉稿用 CSS 还原，没有直接把这些总览图贴入 UI。

下一步：
- 如果后续提供逐个透明 PNG 切片，可继续替换 CSS 绘制的按钮、滑块、开关为真实图片状态。

### 2026-05-26 启动页重做与代码模块拆分

本次目标：
- 使用 `assets/images/startup_page_assets_assets_only_png` 的新切图，按 `设计稿_v1/启动页.png` 重做启动页。
- 将原本集中在 `src/main.ts` 的代码按模块拆分，便于后续维护。

完成内容：
- 将应用入口重构为模块化结构，新增 `src/app` 与 `src/styles` 目录。
- 拆分出应用状态、存档、通用 UI、场景渲染、设置弹窗、Canvas 小游戏等独立模块。
- 重做启动页场景，接入新版遗迹、标题、光点、前景、蘑菇主角、加载条等素材。
- 为启动页补充了加载推进、漂浮、呼吸光、闪烁、前景轻摆等动画效果。
- 调整了启动页进入逻辑：加载完成后再允许继续进入命名页或主菜单。
- 统一修正了入口页标题文案与部分历史编码显示问题。
- 补做了桌面与竖屏场景适配规则，保证 16:9 场景在不同设备上可继续使用。

修改文件：
- `index.html`
- `src/main.ts`
- `src/styles.css`
- `src/app/assets.ts`
- `src/app/constants.ts`
- `src/app/types.ts`
- `src/app/save.ts`
- `src/app/ui.ts`
- `src/app/createApp.ts`
- `src/app/components/settingsModal.ts`
- `src/app/game/CanvasGame.ts`
- `src/app/scenes/bootScene.ts`
- `src/app/scenes/namingScene.ts`
- `src/app/scenes/mainMenuScene.ts`
- `src/app/scenes/levelSelectScene.ts`
- `src/app/scenes/gameScene.ts`
- `src/app/scenes/resultScene.ts`
- `src/app/scenes/galleryScene.ts`
- `src/styles/base.css`
- `src/styles/startup-page.css`
- `src/styles/scenes.css`
- `src/styles/settings.css`
- `开发进度.md`

验证结果：
- 执行 `npx tsc --noEmit`，类型检查通过。
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，生产构建通过。
- 使用无界面 Edge 对本地页面做了桌面和竖屏截图校验，确认新版启动页已正常渲染。

遗留问题：
- 竖屏启动页已做专门适配，但标题区与整体构图距离参考稿仍有进一步精修空间。
- 系统默认 `npm` 仍可能走旧版 Node；如直接用系统 Node 跑 Vite 5，仍可能因 Node 版本过低失败。

下一步：
- 继续细化启动页标题区与移动端表现。
- 逐步把其他页面也按现在的模块化结构继续细修。

### 2026-05-26 启动页反馈修正

本次目标：
- 根据最新反馈修正启动页 logo、进度条、加载文案和跳转行为。

完成内容：
- 启动页 logo 改为直接使用 `assets/images/startup_page_assets_assets_only_png/01_logo_title_text/logo_title_full_group.png`。
- 进度条改为“底线 + 完整条裁切显现”的实现，解决默认状态与 100% 状态贴合不上的问题。
- 将“正在唤醒沉睡的苔原……”从模糊图片文案改为真实文本渲染，提升清晰度。
- 移除启动页上的“继续旅程/轻触唤醒旅程”按钮。
- 当前调试阶段保留启动页停留，不再自动或点击进入主菜单。
- 补做桌面与竖屏下的 logo 位置修正，确保整组 logo 在两类视口都可见。

修改文件：
- `src/app/scenes/bootScene.ts`
- `src/app/createApp.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 执行 `npx tsc --noEmit`，类型检查通过。
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，生产构建通过。
- 使用无界面 Edge 对桌面和竖屏启动页分别截图验证，确认：
- logo 已显示为整组图
- 进度条前后状态已贴合
- 加载文案清晰可读
- 页面上无继续按钮，且不会跳转

遗留问题：
- 启动页目前仍停留在调试态，后续如需改为加载完直接进入主菜单，可在当前结构上直接恢复自动跳转。

下一步：
- 等待新的启动页视觉反馈，或继续进入主菜单/后续页面细化。

### 2026-05-26 启动页背景简化

本次目标：
- 将启动页除红框内容外的碎切图背景改为单张背景图，降低结构复杂度。

完成内容：
- 启动页背景层改为直接使用 `assets/images/startup_page_assets_assets_only_png/05_scene_objects_temp/startup_bg_redbox_removed.png`。
- 保留独立元素为：左右发光精灵、整组 logo、加载条区域、底部主角蘑菇。
- 移除原先分散的遗迹、前景、灯台等碎切图 DOM。
- 同步清理对应的旧样式和无用定位逻辑，减少后续维护成本。

修改文件：
- `src/app/scenes/bootScene.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 执行 `npx tsc --noEmit`，类型检查通过。
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，生产构建通过。

遗留问题：
- 启动页红框保留元素的桌面/竖屏位置仍可继续微调，但背景结构已经简化完成。

下一步：
- 继续根据视觉反馈微调红框保留元素的位置与动效强度。

### 2026-05-26 启动页主角改为帧动画

本次目标：
- 将启动页主角蘑菇从单张静态图改为精灵图帧动画。

完成内容：
- 启动页主角改为使用 `assets/images/startup_page_assets_assets_only_png/03_character/mushroom_skirt_sway_spritesheet_row.png`。
- 按 16 帧横向精灵图接入 CSS `steps(16)` 帧动画。
- 根据反馈去掉上下浮动，只保留静态站位下的逐帧裙摆摆动。

修改文件：
- `src/app/scenes/bootScene.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 已确认首版接入完成。
- 后续根据反馈发现 `background-position` 方案会表现成整张精灵图横向滚动，已改为“裁切容器 + 精灵图平移”的标准逐帧实现。

遗留问题：
- 如后续觉得摆动节奏过快或过慢，可继续微调帧动画时长。

下一步：
- 继续根据启动页整体反馈微调主角动画节奏与站位。

### 2026-05-26 启动页进度条状态图与轨道粒子

本次目标：
- 接入新的 0% / 100% 进度条状态图，并为加载区域补充更自然的环绕粒子动效。

完成内容：
- 进度条底态改为使用 `ui_loading_bar_default.png`。
- 进度条高亮态改为使用 `ui_loading_bar_active.png`，通过裁切逐步显现实现填充过程。
- 新增 36 个混合粒子，粒子类型包含圆点、菱形、星芒、短线、花瓣。
- 粒子改为沿进度条闭合椭圆轨道循环运动，包含顺向/逆向、不同速度、大小、透明度与延迟。
- 保留白金、浅黄、淡橙系发光表现，并加入淡入淡出节奏，避免机械感。

修改文件：
- `src/app/scenes/bootScene.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 执行 `npx tsc --noEmit`，类型检查通过。
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，生产构建通过。

遗留问题：
- 如后续需要更贴近最终设计稿，可继续微调粒子密度、轨道半径和状态图裁切节奏。

下一步：
- 继续根据启动页整体反馈细调进度条区域的视觉强度与节奏。

### 2026-05-27 启动页进度条改为整条帧动画

本次目标：
- 将启动页进度条从状态图裁切方案切换为整条逐帧动画方案。

完成内容：
- 接入 `assets/images/startup_page_assets_assets_only_png/02_ui_loading/04-ui_loading_bar_fill_waterlux_frames` 下的 40 帧资源。
- 启动页进度条改为按帧切图顺序播放，不再使用 `default/active` 状态图裁切。
- 移除上一版围绕进度条的粒子轨道实现，避免与整条帧动画表现冲突。
- 启动时长与帧播放时长对齐，加载结束后停在最后一帧。

修改文件：
- `src/app/constants.ts`
- `src/app/createApp.ts`
- `src/app/scenes/bootScene.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 执行 `npx tsc --noEmit`，类型检查通过。
- 使用 Codex bundled Node 执行 `node node_modules/vite/bin/vite.js build`，生产构建通过。
- 本地预览已确认进度条区域切换为整条帧动画资源。

遗留问题：
- 如后续需要更精确控制帧节奏，可继续按单帧停留时长微调，而不必整体等分 40 帧。

下一步：
- 继续根据启动页视觉反馈微调进度条帧节奏与文案位置。

### 2026-05-27 启动页加载完成态微调

本次目标：
- 修正启动页加载完成后的静态表现。

完成内容：
- 进度条加载完成后固定停留在最后一帧，不再因重新渲染回到首帧。
- logo 入场动画只在加载过程中触发，加载完成后不再重复播放。
- “正在唤醒沉睡的苔原……”文案字号缩小，并向设计稿位置收拢。
- 根据后续反馈再次缩小加载文案字号，并继续上提到更贴近设计稿红框的位置。

修改文件：
- `src/app/scenes/bootScene.ts`
- `src/styles/startup-page.css`
- `开发进度.md`

验证结果：
- 待本轮构建与页面确认。

遗留问题：
- 如还需更贴设计稿，可继续细调文案与进度条的垂直间距。
- 根据后续反馈，文案位置与进度条留白区域仍在继续优化中。

下一步：
- 根据最新视觉反馈继续细修启动页加载区。

## 协作约定

- 后续每次有实际开发改动，我都会同步更新本文件，方便跨设备续接。
